doctype html
html
	head
	title Histograma
	body
		-var duom = duomenys
		script(type='text/javascript').
			var arr = new Array();
			var count = 0;
			duomenys.forEach(function(element){
			 arr[i] =	{y: element.Kiekis,	label: element.Data};
			 count = count + 1;
			})
		td='#{count}'//Neispausdina arr i jade, gali but tuscias mas	
		script(type='text/javascript').
			var arr = new Array();
			var count = 0;
			duomenys.forEach(function(element){
			 arr[i] =	{y: element.Kiekis,	label: element.Data};
			 count = count + 1;
			})
			var galDuom = JSON.stringify(arr)
			window.onload = function () {
			var chart = new CanvasJS.Chart("chartContainer", {
			theme: "light1", // "light2", "dark1", "dark2"
			animationEnabled: false, // change to true
			title:{
			text: "Isilauzimu Histograma"
			},
			data: [
			{
			// Change type to "bar", "area", "spline", "pie", column etc. !{data} '#{duomenys}'
			type: "column",
			dataPoints: '!{galDuom}'
			}
			]
			});
			chart.render();
			}
		#chartContainer(style='height: 370px; width: 100%;')
		script(src='https://canvasjs.com/assets/script/canvasjs.min.js')
